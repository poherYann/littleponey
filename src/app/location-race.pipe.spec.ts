/*
import { LocationRacePipe } from './location-race.pipe';

describe('LocationRacePipe', () => {
  it('create an instance', () => {
    const pipe = new LocationRacePipe();
    expect(pipe).toBeTruthy();
  });
  it('change string location', function () => {
    const

  });
});
*/
