/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UpdatePonyReactComponent } from './update-pony-react.component';

describe('UpdatePonyReactComponent', () => {
  let component: UpdatePonyReactComponent;
  let fixture: ComponentFixture<UpdatePonyReactComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ UpdatePonyReactComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(UpdatePonyReactComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
